type Person = {
    height: number,
    weight: number,
    gender?: string // optional
}

let salman: Person = {
    height: 6,
    weight: 70,
    gender: "male"
}

let two: Person = {
    height: 6,
    weight: 70,
    // gender is optional
}